# Model Router service dependencies.
# All versions pinned for reproducible builds.
# Never use open ranges like >=x.y — silent upgrades can introduce breaking
# changes or behaviour differences that are hard to trace back to a library.

# Async Kafka client — consumer + producer with manual offset commit support.
aiokafka==0.10.0

# PostgreSQL async driver — connection pool with per-connection server_settings.
asyncpg==0.29.0

# Settings validation from environment variables — fails fast on missing vars.
pydantic-settings==2.3.0

# Structured JSON logging.
structlog==24.1.0

# Prometheus metrics exposition — Counter, start_http_server.
prometheus-client==0.20.0

# Retry with exponential backoff for Kafka publish (Circuit Breaker).
tenacity==8.3.0

# --- Test dependencies (pinned for CI reproducibility) ---

# Unit testing framework.
pytest==8.2.0

# pytest plugin for running async test functions natively with asyncio.
pytest-asyncio==0.23.7
