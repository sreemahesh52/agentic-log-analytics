# postgres package — contains TenantRepository for all PostgreSQL queries.
# Importing as a package allows `from postgres.repository import TenantRepository`
# inside the service without path manipulation.
