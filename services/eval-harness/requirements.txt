# Async Kafka consumer for consuming from agent.results topic.
# aiokafka is consistent with the semantic-cache, api-gateway, and other async services.
aiokafka==0.10.0

# PostgreSQL async driver — pool with server_settings timezone=UTC.
asyncpg==0.29.0

# Async Redis client for the SemanticCacheWriter.
# redis.asyncio is the official async interface bundled with the redis package.
redis==5.0.6

# OpenAI client for GPT-3.5 faithfulness and hallucination evaluation.
openai==1.40.0

# httpx 0.27.2 is the last version compatible with openai 1.40.0 (openai passes
# `proxies` to httpx.AsyncClient which was removed in httpx 0.28+).
httpx==0.27.2

# NumPy for cosine similarity computation (used by SemanticCacheWriter).
# numpy<2 required: chromadb 0.4.22 uses np.float_ which was removed in NumPy 2.0.
numpy==1.26.4

# Vector database for similarity strategy (past_incidents collections).
chromadb==0.4.22

# Pydantic v2 for config validation and data contracts.
pydantic==2.7.4
pydantic-settings==2.3.0

# Prometheus metrics exposure on port 8091.
prometheus-client==0.20.0

# Structured JSON logging.
structlog==24.1.0

# .env file loading for local development.
python-dotenv==1.0.0

# Test framework.
pytest==8.2.2

# Required for pytest to run async test functions with @pytest.mark.asyncio.
pytest-asyncio==0.23.7

# In-memory Redis for cache tests (matching other services).
fakeredis==2.23.2
