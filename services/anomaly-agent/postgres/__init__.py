# postgres package — contains repository classes for PostgreSQL access.
# All SQL for the anomaly-agent lives in repository.py, never in the orchestrator.
# This is the Repository Pattern: one class per domain concern, SQL isolated here.
